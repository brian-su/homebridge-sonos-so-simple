{
    "displayName": "Sonos So Simple",
    "name": "homebridge-sonos-so-simple",
    "version": "0.2.6",
    "description": "A very simple plugin for controlling Sonos devices via Homebridge.",
    "license": "Apache-2.0",
    "repository": {
        "type": "git",
        "url": "git://github.com/brian-su/homebridge-sonos-so-simple.git"
    },
    "bugs": {
        "url": "https://github.com/brian-su/homebridge-sonos-so-simple/issues"
    },
    "engines": {
        "node": ">=12.22.7",
        "homebridge": ">=1.0.0"
    },
    "main": "dist/index.js",
    "scripts": {
        "prettify": "prettier --write 'src/*.{js,ts,json}' '*.json' 'src/services/*.ts'",
        "watch": "npm run build && npm link && nodemon",
        "build": "rimraf ./dist && tsc",
        "prepublishOnly": "npm run build",
        "localDev": "npm run build && npm link && homebridge -D -I",
        "trackDev": "npm run watch"
    },
    "keywords": [
        "homebridge-plugin"
    ],
    "dependencies": {
        "sonos": "^1.14.1"
    },
    "devDependencies": {
        "@types/node": "^14.14.22",
        "@typescript-eslint/eslint-plugin": "^4.14.0",
        "@typescript-eslint/parser": "^4.14.0",
        "homebridge": "^1.2.5",
        "nodemon": "^2.0.7",
        "prettier": "^2.2.1",
        "rimraf": "^3.0.2",
        "ts-node": "^9.1.1",
        "typescript": "^4.1.3"
    },
    "overrides": {
        "minimist": ">=1.2.6"
    }
}
